export const PUBLIC_PATHS = ["/", "/explore", "/u/"] as const;
export const AUTH_PATHS = ["/auth/sign-in", "/auth/sign-up"] as const;
export const AUTH_API_PREFIX = "/api/auth" as const;
export const UT_API_PREFIX = "/api/uploadthing" as const;
export const PROTECTED_PATHS = ["/settings"] as const;
